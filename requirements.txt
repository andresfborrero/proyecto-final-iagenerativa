# Core LangChain packages (compatible range)
langchain>=0.2.7,<0.3
langchain-community>=0.2.7,<0.3
# Let langchain bring the correct core version
# langchain-core is intentionally not pinned here
langchain-text-splitters>=0.2.0,<0.3

# Google Gemini integration for LangChain
langchain-google-genai>=1.0.3,<2
google-generativeai>=0.7,<1

# Vector stores
# Try FAISS where available; on Windows we will fallback to Chroma in code
faiss-cpu>=1.7.4; platform_system != "Windows"
chromadb>=0.5,<0.6

# Runtime and compatibility
pydantic>=2,<3
typing-extensions>=4.8.0
numpy<2

# UI
streamlit>=1.32,<2
